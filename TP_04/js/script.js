const mostrarError = ()=> console.error("un msj de error");
// = () => 

const sumar = (x,y) => (x+y);

const cuadrado = x => (x*x);

const alumno = {
    apellido: "titos",
    localidad: "caleta olivia",
    edad: 21,
    calcularEdad: ()=> {
        //calc edad
    },

    save: ()=>{},
    update: ()=>{},
    delete: ()=> {}
};

const getAlumno = id => ({
    apellido: "titos",
    localidad: "caleta olivia",
    
})
// ARREGLOS 

let colores = new Array(5);
